# Edge Dockerfile (stub)
# TODO: Implement in Module 16

FROM python:3.12.8-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application
COPY src/ ./src/

# Expose port
EXPOSE 8000

# Run with hypercorn
CMD ["hypercorn", "src.app.main:app", "--bind", "0.0.0.0:8000", "--workers", "1"]
